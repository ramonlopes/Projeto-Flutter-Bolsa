Instruções rápidas (Windows emulador Android):
1. Abra o Android Emulator ou conecte um dispositivo.
2. Ajuste o backend: por padrão o app usa http://10.0.2.2:3000/ (Android emulator -> host machine).
3. Rode o app:
   flutter pub get
   flutter run
